<h2 class="page-header"><%= author.name %></h2>
<div class="btn-row">
  <comment: author.id that way we edit the corrct author>  
  <a class="btn btn-primary" href="/authors/<%= author.id %>/edit">Edit</a>
  <comment: inculdes partial delete form > 
  <%- include('../partials/deleteForm',
              { url: `/authors/${author.id}` }) %>
</div>
<% if (booksByAuthor.length > 0) { %>
  <h2 class="page-header">Books By Author</h2>
  <comment: This partial encapsulates how the books should be displayed.
  It is incuded from |partials|bookGrid >
  <%- include("../partials/bookGrid", {books: booksByAuthor})%>
<% } %>