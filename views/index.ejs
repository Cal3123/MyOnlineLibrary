<h2 class="page-header">Recently Added</h2>
<div>  
    <comment: renders all our different books as imgaes as we want to>
    <% books.forEach(book => { %>
        <img height="150" width ="100" src = "<%=book.coverImagePath%>">
   <% }) %>
   </div>